#
# >>> Escriba el codigo del mapper a partir de este punto <<<
#
import sys

for linea in sys.stdin:
  lineas = row.split(",")
  amount = elementos[4]
  purpose = elementos[3]
  r = purpose + ";" + cantidad
  sys.stdout.write(r+"\n")
